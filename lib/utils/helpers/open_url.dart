import 'package:bot_toast/bot_toast.dart';


openUrl(String url) async {
  var uri = Uri.parse(url);
  // if (await canLaunchUrl(uri)) {
  //   launchUrl(uri,mode: LaunchMode.externalApplication);
  // } else {
  //   BotToast.showText(text:'Không thể mở link');
  // }
}
